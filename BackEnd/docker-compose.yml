version: "3"
services:
  app:
    image: mhart/alpine-node:8
    container_name : app
    ports:
      - "8080:8080"
    command: "npm start"  
    volumes:
      - ./:/app
    working_dir: /app    
  mongo:
    container_name: mongodb
    image: mongo     